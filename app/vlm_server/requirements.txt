fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
# transformers>=4.52 requires torch>=2.5 (torch.distributed.tensor.DTensor) —
# the base image pins torch==2.4.0, so cap below that to avoid an import
# crash at startup.
transformers>=4.45,<4.52
accelerate>=0.34
bitsandbytes>=0.43
pillow>=10.0
einops>=0.8
timm>=1.0
sentencepiece>=0.2
# InternVL2.5's tokenizer (tokenization_internlm2.py, sentencepiece-based)
# needs this for its error-handling import path even when nothing fails.
protobuf>=4.25
# DeepSeek-VL (v1, deepseek-ai/deepseek-vl-7b-chat) ships its own model
# classes (VLChatProcessor, MultiModalityCausalLM) that AutoModel/AutoConfig
# don't know about until this package is imported (it self-registers as an
# import side effect) — the generic trust_remote_code path 404s on the
# "multi_modality" model_type without it. Installed in the Dockerfile from a
# tarball snapshot (no PyPI release, and no git+https here to avoid needing
# a git binary in the image) rather than listed here.
